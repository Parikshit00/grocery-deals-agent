You are reading a screenshot of a German PENNY weekly offers page ("Angebote"), shown as a grid of
product cards. Extract every concrete product offer fully visible on this screenshot.

Return ONLY JSON, no commentary:
{"offers":[{"product_name": string, "brand": string|null, "price": number|null, "old_price": number|null, "unit": string|null, "description": string|null}]}

Rules:
- Prices are in EUR; the comma is the decimal separator ("1,99" -> 1.99).
- "price" is the current offer price; "old_price" is the crossed-out / UVP / "statt" price if shown, else null.
- "unit" is the printed base unit if any (e.g. "kg", "l", "Stück", "100 g").
- Skip cards that are cut off at the top or bottom edge (they appear again on an adjacent screenshot).
- Ignore non-product content: ads, recipes, category headers, app/loyalty banners, page chrome.
- If the screenshot shows no product offers, return {"offers":[]}.
